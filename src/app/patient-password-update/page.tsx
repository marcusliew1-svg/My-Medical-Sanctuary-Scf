import { PatientAccessShell } from "@/components/PatientAccessShell";
export default async function Page({ searchParams }: { searchParams?: Promise<{ error?: string }> }) { const query = await searchParams; return <PatientAccessShell eyebrow="Account security" title="Choose a new password." lead="Use at least 12 characters and avoid passwords used for other services.">{query?.error ? <p role="alert" className="mb-5 border-l-2 border-gold bg-ivory px-4 py-3 text-sm text-navy">The link is invalid or the passwords could not be accepted.</p> : null}<form action="/api/patient-auth/password-update" method="post" className="grid gap-5"><label className="grid gap-2 text-sm font-semibold text-navy">New password<input name="password" type="password" minLength={12} maxLength={128} required autoComplete="new-password" className="rounded-xl border border-stone-200 px-4 py-3 font-normal outline-none focus:border-gold" /></label><label className="grid gap-2 text-sm font-semibold text-navy">Confirm password<input name="confirmPassword" type="password" minLength={12} maxLength={128} required autoComplete="new-password" className="rounded-xl border border-stone-200 px-4 py-3 font-normal outline-none focus:border-gold" /></label><button className="min-h-12 rounded-full bg-deep-green px-6 font-semibold text-white">Update password</button></form></PatientAccessShell>; }
